import React from "react";
import { createRoot } from "react-dom/client";
import { AnimatePresence, motion } from "framer-motion";

const ReactDOM = { createRoot };
const Motion = { AnimatePresence, motion };

export { Motion, React, ReactDOM };
